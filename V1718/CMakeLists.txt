if (UNIX)
    add_subdirectory(linux)
    else if (WIN32)
        if( ${BITS}  STREQUAL "64")
            add_subdirectory(windows/x64)
        else()
            add_subdirectory(windows/x86)
        endif()
    endif (WIN32)
endif (UNIX)
